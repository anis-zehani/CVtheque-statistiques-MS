spring:
  application:
    name: Statistiques-MS
  cloud:
    config:
      discovery:
        enabled: true
        service-id: Config-MS

management:
  endpoints:
    web:
      exposure:
        include: ["health", "info", "refresh"]